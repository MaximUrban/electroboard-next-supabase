"use client";

import React from "react";

export default function ElectroBoard() {
  return (
    <div
      style={{
        minHeight: "100vh",
        background: "#0b1020",
        color: "#f2f6ff",
        padding: 24,
        fontFamily: "Inter, Arial, sans-serif",
      }}
    >
      <h1 style={{ marginTop: 0 }}>ElectroBoard</h1>
      <p>Базовый экран загружен. Следующим сообщением вернём полный редактор без ошибки сборки.</p>
    </div>
  );
}
